[project]
name = "codecrafters-bittorrent"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "bencode-py>=4.0.0",
    "requests>=2.32.5",
]
[tool.pytest.ini_options]
pythonpath = ["app"] #adds app/ in front of actual files to prevent file not found when running pytest

